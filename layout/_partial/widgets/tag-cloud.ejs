<div class="sidebar-widget">
  <p class="sidebar-widget__title">Tags</p>
  <div class="tag-cloud">
    <% site.tags.sort('name').each(tag => { %>
      <a href="<%- url_for(tag.path) %>" class="tag-pill">
        <%= tag.name %> <span class="tag-cloud__count">(<%= tag.length %>)</span>
      </a>
    <% }) %>
  </div>
</div>
